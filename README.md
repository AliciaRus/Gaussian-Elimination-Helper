# Gaussian-Elimination-Demonstration
Performs Gaussian Elimination row operations on a matrix. Helpful for finding the row-reduced form of a matrix via successive row operations by reducing tedious arithmetic calculations.

* Gaussian Elimination can be used to solve a system of equations.

## Three Helper Functions
* addRows: adds two rows
* scaleRow: scales a row by a given factor
* swapRows: swaps two rows
